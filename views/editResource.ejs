<!DOCTYPE html>
<html>
<head>
  <title>Edit Resource</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body { background-color: #f4f1ea; }
    .btn-brown { background-color: #4B2E2E; color: white; }
  </style>
</head>
<body>
  <div class="container mt-5">
    <a href="/resources" class="btn btn-sm btn-outline-secondary mb-3">← Back to Resources</a>

    <div class="card shadow p-4">
      <h3>Edit Resource</h3>
      <form action="/editResource/<%= selected.id %>" method="POST">
        <div class="form-row">
          <div class="form-group col-md-2">
            <label>Subject ID</label>
            <input type="text" name="subject_id" class="form-control" value="<%= selected.subject_id %>" required>
          </div>
          <div class="form-group col-md-4">
            <label>Title</label>
            <input type="text" name="title" class="form-control" value="<%= selected.title %>" required>
          </div>
          <div class="form-group col-md-3">
            <label>Type</label>
            <select name="type" class="form-control" required>
              <option value="video" <%= selected.type === 'video' ? 'selected' : '' %>>Video</option>
              <option value="article" <%= selected.type === 'article' ? 'selected' : '' %>>Article</option>
              <option value="document" <%= selected.type === 'document' ? 'selected' : '' %>>Document</option>
              <option value="quiz" <%= selected.type === 'quiz' ? 'selected' : '' %>>Quiz</option>
              <option value="case_study" <%= selected.type === 'case_study' ? 'selected' : '' %>>Case Study</option>
            </select>
          </div>
          <div class="form-group col-md-3">
            <label>URL</label>
            <input type="url" name="url" class="form-control" value="<%= selected.url %>" required>
          </div>
        </div>
        <button class="btn btn-brown">Save Changes</button>
      </form>
    </div>
  </div>
</body>
</html>