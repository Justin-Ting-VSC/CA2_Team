<!DOCTYPE html>
<html>
<head>
  <title>View Resource</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body { background-color: #f4f1ea; }
    .btn-brown { background-color: #4B2E2E; color: white; }
  </style>
</head>
<body>
  <div class="container mt-5">
    <a href="/resources" class="btn btn-sm btn-outline-secondary mb-3">← Back to Resources</a>

    <div class="card shadow p-4">
      <h3><%= selected.title %></h3>
      <p><strong>Subject ID:</strong> <%= selected.subject_id %></p>
      <p><strong>Type:</strong> <%= selected.type %></p>
      <p><strong>Created At:</strong> <%= (selected.created_at instanceof Date ? selected.created_at : new Date(selected.created_at)).toISOString().slice(0,19).replace('T',' ') %></p>

      <hr>
      <p><strong>Resource Link:</strong>
        <a href="<%= selected.url %>" target="_blank" class="btn btn-brown">Open Resource</a>
      </p>
    </div>
  </div>
</body>
</html>