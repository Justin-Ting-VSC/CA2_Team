<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <nav style="background-color: #4B2E2E;" class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard" style="color: #F5F5DC; font-weight: bold;">Homepage</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon" style="filter: invert(1);"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#" style="color: #F5F5DC;">Timetable</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" style="color: #F5F5DC;">Study Resources</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" style="color: #F5F5DC;">Subjects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" style="color: #F5F5DC;">Study Groups</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" style="color: #F5F5DC;">Exam Schedules</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <style>
        .navbar {
            border-radius: 0 0 10px 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        .nav-link {
            transition: background 0.2s, text-decoration 0.2s;
            margin-right: 0.5rem;
        }
        .nav-link:hover {
            background-color: #5C3A3A;
            color: #FFF;
            text-decoration: underline;
            border-radius: 5px;
        }
        .navbar-brand {
            font-size: 1.2rem;
        }
    </style>

    
    <div class="container">
        <h1 class="mt-5">Dashboard</h1>
        <p>Welcome, <%= user.username %> (<%= user.role %>)</p>
        <% if (user.role === 'admin') { %>
            <a href="/admin" class="btn btn-primary">Admin Dashboard</a>
        <% } %>
        <a href="/logout" class="btn btn-primary">Logout</a>
    </div>

</body>
</html>
