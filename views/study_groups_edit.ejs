<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Study Groups Edit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            font-family: "SF Pro Display Bold", "SF Pro Display", "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
            background-color: #e6dfd0;
            color: #343a40;
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .navbar {
            background-color: #4B2E2E !important;
            border-radius: 0 0 10px 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            margin-bottom: 20px;
        }

        .nav-link {
            transition: background 0.2s, text-decoration 0.2s;
            margin-right: 0.5rem;
            color: #F5F5DC !important;
        }

        .nav-link:hover, .nav-link.active {
            background-color: #5C3A3A !important;
            color: #FFF !important;
            text-decoration: underline;
            border-radius: 5px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        h2 {
            color: #19202f;
            margin-bottom: 30px;
        }

        form label {
            font-weight: 600;
            margin-top: 10px;
        }

        form input[type="text"],
        form input[type="number"] {
            width: 100%;
            padding: 10px 12px;
            border-radius: 8px;
            border: 1px solid #ced4da;
            margin-top: 5px;
            margin-bottom: 15px;
            background-color: #fff;
            color: #343a40;
            font-size: 1rem;
            box-sizing: border-box;
        }

        form button {
            background-color: #E6B614;
            color: #2C1810;
            border: none;
            border-radius: 8px;
            padding: 10px 25px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
            white-space: nowrap;
        }

        form button:hover {
            background-color: #d4af0a;
            color: #1e1208;
        }

        a.btn-back {
            display: inline-block;
            margin-top: 20px;
            color: #19202f;
            border: 1px solid #19202f;
            padding: 8px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: background-color 0.3s ease, color 0.3s ease;
            white-space: nowrap;
        }

        a.btn-back:hover {
            background-color: #19202f;
            color: #ffffff;
            text-decoration: none;
        }

        .btn-add-plan {
            background-color:#4B2E2E; 
            color:#F5F5DC; 
            border:none; 
            border-radius:8px; 
            padding:6px 12px; 
            font-weight:600; 
            text-decoration:none;
            white-space: nowrap;
        }

        .btn-add-plan:hover {
            background-color: #5C3A3A;
            color: #FFF;
            text-decoration: none;
        }

        .text-center {
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><b>📚 Study Buddy</b></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/timetable">Timetable</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/subjects">Subjects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/study_groups">Study Groups</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/exams">Exams</a>
                    </li>
                    <li class="nav-item ms-3">
                        <a href="/study_groups_add" class="btn btn-add-plan">📄 Add a new plan</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="text-center">
            <h2>Edit details</h2>
        </div>

        <% if (study_groups) { %> 
        <form action="/study_groups_edit/<%= study_groups.id %>" method="POST" novalidate>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= study_groups.name || '' %>" required />

            <label for="subject_id">Subject ID:</label>
            <input type="number" id="subject_id" name="subject_id" value="<%= study_groups.subject_id || 0 %>" required />

            <label for="members">Members:</label>
            <input type="text" id="members" name="members" value="<%= study_groups.members || '' %>" required />

            <button type="submit">✏️ Update</button>
        </form>
        <% } else { %>
            <p class="text-center">No groups found.</p>
        <% } %>

        <a href="/study_groups" class="btn-back">🔙 Back</a>
    </div>
</body>
</html>
