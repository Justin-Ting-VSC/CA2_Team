<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Supermarket App</title>
    </head>
<body>

    <div class="container">
        <br>
        <div class="text-center"><h2>Edit details</h2></div>
        <br>
        <div class="text-center">
          <% if (study_groups) { %> 
            <form action="/study_groups_edit/<%= study_groups.id %>" method="POST">
                <label for="name">Name:</label><br>
                <input type="text" id="name" name="name" value=<%= study_groups.name || '' %> required><br><br>

                <label for="subject_id">Subject ID:</label><br>
                <input type="number" id="subject_id" name="subject_id" value=<%= study_groups.subject_id || 0 %> required><br><br>

                <label for="members">Members:</label><br>
                <input type="text" id="members" name="members" value=<%= study_groups.members || '' %> required><br><br>

                <button type="submit">Update</button>
            </form>
          <% } else { %>
              <p>No groups found.</p>
          <% } %>
          <a href ="/study_groups">Back</a>
        </div>
    </div>
</body>
</html>